# قائمة التحقق من النشر 🔍

## مرحلة ما قبل النشر

### ✅ 1. إعدادات المشروع
- [ ] إنشاء ملف `.env` من `env_template.txt`
- [ ] تعيين `SECRET_KEY` عشوائي وآمن
- [ ] تعيين `DEBUG=False`
- [ ] إضافة نطاق الموقع إلى `ALLOWED_HOSTS`
- [ ] إعداد قاعدة البيانات PostgreSQL

### ✅ 2. قاعدة البيانات
- [ ] إنشاء قاعدة بيانات PostgreSQL
- [ ] تشغيل `python manage.py migrate`
- [ ] إنشاء مستخدم إدارة (`python manage.py createsuperuser`)

### ✅ 3. الملفات الثابتة
- [ ] تشغيل `python manage.py collectstatic`
- [ ] التحقق من ملفات `staticfiles/`

### ✅ 4. الأمان
- [ ] التحقق من `dawerha.production_settings.py`
- [ ] تعيين `DEBUG=False`
- [ ] تمكين HTTPS
- [ ] تعيين `SECURE_SSL_REDIRECT=True`

### ✅ 5. المراقبة
- [ ] إعداد نظام السجلات
- [ ] إعداد المراقبة
- [ ] إعداد النسخ الاحتياطي

---

## مرحلة النشر

### ✅ 1. رفع الملفات
- [ ] رفع جميع الملفات إلى الخادم
- [ ] التأكد من `.env` في المكان الصحيح
- [ ] التأكد من أذونات الملفات

### ✅ 2. تثبيت المتطلبات
```bash
pip install -r requirements.txt
```

### ✅ 3. إعداد قاعدة البيانات
```bash
python manage.py migrate
python manage.py createsuperuser
```

### ✅ 4. جمع الملفات الثابتة
```bash
python manage.py collectstatic
```

### ✅ 5. تشغيل الخادم
```bash
# باستخدام Gunicorn
gunicorn dawerha.wsgi:application

# أو استخدم start_production.sh
bash start_production.sh
```

---

## مرحلة ما بعد النشر

### ✅ 1. الاختبار
- [ ] اختبار الصفحة الرئيسية
- [ ] اختبار تسجيل الشركات
- [ ] اختبار لوحة الإدارة
- [ ] اختبار لعبة العجلة
- [ ] اختبار الجدولة التلقائية

### ✅ 2. الأمان
- [ ] التحقق من HTTPS
- [ ] التحقق من حماية CSRF
- [ ] التحقق من كلمات المرور
- [ ] التحقق من السجلات

### ✅ 3. الأداء
- [ ] مراقبة استخدام الذاكرة
- [ ] مراقبة سرعة الاستجابة
- [ ] مراقبة قاعدة البيانات

---

## إعدادات Gunicorn المقترحة

```bash
gunicorn dawerha.wsgi:application \
    --bind 0.0.0.0:8000 \
    --workers 3 \
    --worker-class gthread \
    --threads 2 \
    --worker-connections 1000 \
    --max-requests 1000 \
    --max-requests-jitter 100 \
    --timeout 30 \
    --keep-alive 2 \
    --preload \
    --log-level info
```

---

## استكشاف الأخطاء

### مشكلة: البيانات لا تُحفظ
**الحل:** تحقق من أذونات قاعدة البيانات

### مشكلة: الملفات الثابتة لا تظهر
**الحل:** شغّل `collectstatic` وتحقق من `STATIC_ROOT`

### مشكلة: الأخطاء 500
**الحل:** راجع السجلات وتحقق من إعدادات `DEBUG`

### مشكلة: الجدولة لا تعمل
**الحل:** تأكد من أن Middleware يعمل بشكل صحيح

---

## نصائح مهمة

1. **لا تشغّل Debug في الإنتاج أبداً**
2. **استخدم HTTPS دائماً**
3. **احتفظ بنسخة احتياطية من قاعدة البيانات**
4. **راقب السجلات بانتظام**
5. **حدّث المكتبات بانتظام**

---

## الاتصال

في حالة المشاكل، تواصل معنا:
- البريد: info@dawwerha.com
- الواتساب: +966573663977

