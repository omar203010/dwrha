# قائمة التحقق قبل الرفع على GitHub

## ✅ التحقق من الأمان

- [ ] تأكد من عدم وجود ملف `.env` في المشروع
- [ ] تأكد من أن `.gitignore` يحتوي على `.env` و `db.sqlite3` و `*.log`
- [ ] لا توجد معلومات حساسة (كلمات مرور، مفاتيح API) في الكود
- [ ] لا توجد URLs ثابتة مثل `http://127.0.0.1:8000` في الكود (تم استبدالها)

## ✅ الملفات المطلوبة

- [x] `.gitignore` - محدث ومكتمل
- [x] `env.example` - قالب للمتغيرات البيئية
- [x] `SECURITY.md` - دليل الأمان
- [x] `CONTRIBUTING.md` - دليل المساهمة
- [x] `README.md` - محدث مع تعليمات الأمان

## ✅ الكود

- [x] الكود نظيف ومرتب
- [x] لا توجد أخطاء في `python manage.py check`
- [x] جميع الواردات (imports) مرتبة
- [x] لا توجد معلومات حساسة في الكود

## ✅ قاعدة البيانات

- [ ] تأكد من عدم رفع `db.sqlite3` (موجود في `.gitignore`)
- [ ] تأكد من رفع جميع ملفات migrations

## ✅ الملفات الثابتة

- [ ] ملفات `staticfiles/` لا يتم رفعها (موجودة في `.gitignore`)
- [ ] ملفات `media/` لا يتم رفعها (موجودة في `.gitignore`)

## 📝 خطوات ما قبل الرفع

1. تحقق من جميع العناصر أعلاه
2. قم بتشغيل: `python manage.py check`
3. تأكد من أن المشروع يعمل محلياً
4. ارفع الملفات باستخدام:
   ```bash
   git add .
   git commit -m "Initial commit - Clean and secure codebase"
   git push
   ```

## ⚠️ ملاحظات مهمة

- **لا ترفع ملف `.env`** - يحتوي على معلومات حساسة
- **لا ترفع قاعدة البيانات** - `db.sqlite3` موجود في `.gitignore`
- **لا ترفع ملفات السجلات** - `*.log` موجود في `.gitignore`
- استخدم `env.example` كقالب عند إعداد المشروع على خادم جديد

